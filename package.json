{
  "name": "my-project",
  "version": "0.1.0",
  "homepage": "https://sjoerdvink99.github.io/portfolio/",
  "private": true,
  "dependencies": {
    "react": "^18.3.1",
    "react-dom": "^18.3.1"
  },
  "scripts": {
    "start": "bun run build && bun dev.ts",
    "build": "bun build-simple.ts && bun build src/index.tsx --outdir build --target browser --minify",
    "build:deploy": "bun build-simple.ts --deploy && bun build src/index.tsx --outdir build --target browser --minify",
    "deploy": "bun run build:deploy && gh-pages -d build",
    "typecheck": "tsc --noEmit"
  },
  "eslintConfig": {
    "extends": [
      "react-app",
      "react-app/jest"
    ]
  },
  "browserslist": {
    "production": [
      ">0.2%",
      "not dead",
      "not op_mini all"
    ],
    "development": [
      "last 1 chrome version",
      "last 1 firefox version",
      "last 1 safari version"
    ]
  },
  "devDependencies": {
    "@types/node": "^25.5.2",
    "@types/react": "^19.2.14",
    "@types/react-dom": "^19.2.3",
    "gh-pages": "^6.3.0",
    "tailwindcss": "^3.4.19",
    "typescript": "^5.9.3"
  }
}
