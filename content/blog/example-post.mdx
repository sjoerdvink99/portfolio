---
title: "Human-AI Interaction"
date: "2026-04-29"
description: "A short example showing what a blog post looks like in this setup."
---

This is the opening paragraph. It gives readers a quick sense of what the post is about before diving into the details.

## Section One

Here is some body text. You can write in plain Markdown — paragraphs, **bold**, _italic_, `inline code`, and [links](https://example.com) all work out of the box.

- Item one
- Item two
- Item three

## Code Example

```python
def greet(name: str) -> str:
    return f"Hello, {name}!"

print(greet("world"))
```

## Conclusion

Wrap up with a closing thought. That's all there is to it — drop a `.mdx` file in `content/blog/` with the three frontmatter fields (`title`, `date`, `description`) and it will appear in the blog listing automatically.
